# Wallet app docs

Reference material for `wallet_ethcali`. The contracts themselves live in
`scs-ethcali`; this folder documents how the app talks to them.

## In this folder

| Doc | What it covers |
|-----|----------------|
| [ARCHITECTURE.md](ARCHITECTURE.md) | Pages, hooks, utility and config layers, networks |
| [COMPONENT_GUIDELINES.md](COMPONENT_GUIDELINES.md) | Component conventions |
| [FRONTEND_CHANGES.md](FRONTEND_CHANGES.md) | Change log for the frontend |
| [FAUCET_CONTRACT_REFERENCE.md](FAUCET_CONTRACT_REFERENCE.md) | FaucetManager API as the app uses it |
| [ZKPASSPORT_CONTRACT_REFERENCE.md](ZKPASSPORT_CONTRACT_REFERENCE.md) | ZKPassportNFT API as the app uses it |
| [HACKATHON_STAKING_ARCHITECTURE.md](HACKATHON_STAKING_ARCHITECTURE.md) | HackathonStaking design notes |
| [SECURITY_ADMIN_GUIDE.md](SECURITY_ADMIN_GUIDE.md) | Roles and admin operations |
| [SWAG_ORDERS.md](SWAG_ORDERS.md) | Swag orders: crypto purchase, Shopify webhook, claim vouchers, `/swag/admin` runbook, env |

## Swag1155

The Swag1155 reference is maintained once, in the contracts repo, next to the
Solidity it describes:

- [`../../scs-ethcali/docs/SWAG1155_CONTRACT_REFERENCE.md`](../../scs-ethcali/docs/SWAG1155_CONTRACT_REFERENCE.md)

Do not copy it here. The wallet consumes the contract through the typed ABI in
`frontend/abis/swag.ts`, regenerated by `npm run sync:contracts`, so a stale
copy of the reference would only disagree with what the compiler enforces.

The product rules the app follows (one tokenId per design, split caps, USDC
only, one chain — Ethereum mainnet — size on the order) are in the workspace spec:
`../../docs/swag-rebuild-spec.md`.

## Contract bindings

`frontend/` is generated. `npm run sync:contracts` copies `abis/*.json`,
`addresses.json` and `contracts.ts` from `../scs-ethcali/frontend`, emits the
typed `abis/swag.ts`, reads the live collection from
`../scs-ethcali/deployments/<SWAG_CHAIN>-latest.json` (`ethereum` by default)
into `swag-collection.json`, and records the source commit in
`CONTRACTS_SOURCE.json`. Commit the outputs so
Vercel builds without the sibling checkout.
